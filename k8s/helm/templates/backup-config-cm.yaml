apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8s-backup-operator.name" . }}-backup-config
  labels:
    {{- include "k8s-backup-operator.labels" . | nindent 4 }}
data:
  retryTimeLimit: "{{ .Values.backup.retryTimeLimit }}"
